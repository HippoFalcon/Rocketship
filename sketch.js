function setup() {
  createCanvas(windowWidth,windowHeight)
  background(0,0,0);
}

function draw() {
  var x = mouseX;
  var y = mouseY;
  //Rectangular Body
  fill(198, 196, 192)
  rect(x, y, 45, 100);

}
